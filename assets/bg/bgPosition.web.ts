// Web only: match Expo Go's resizeMode="cover" behavior.
// Expo Go centers cover images by default, so web must also keep the image centered.
export const bgTopAnchor = {
  backgroundPosition: 'center center',
  objectPosition: 'center center',
};

export const bgTopAnchorImg = {
  objectPosition: 'center center',
};
