/**
 * SocialLogin — not used in the Customer App.
 * Customer auth is phone + OTP only (no Google/Facebook login per design brief).
 * This file exists as a placeholder for future implementation if required.
 */
export default function SocialLogin() {
  return null;
}